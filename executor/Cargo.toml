
[package]
name = "executor"
version = "0.1.0"
edition = "2021"

[dependencies]
fyrox = { workspace = true }
something = { path = "../game", optional = true }

[features]
default = ["something"]
dylib = ["fyrox/dylib"]
